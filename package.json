{
  "name": "agentfeed-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm -C packages/server dev",
    "dev:web": "pnpm -C packages/server dev:web",
    "build:web": "pnpm -C packages/server build:web",
    "start": "pnpm -C packages/server start"
  }
}
